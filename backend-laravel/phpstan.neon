includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    # Nível máximo exigido pela skill php-pro: generics de relações Eloquent,
    # value-types de arrays e proibição de mixed sem narrowing explícito.
    level: 9
    paths:
        - app
    treatPhpDocTypesAsCertain: false
