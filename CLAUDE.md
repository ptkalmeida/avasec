@AGENTS.md

# Notas específicas do Claude Code

- As instruções acima (`AGENTS.md`) são a fonte única da verdade. Não duplique regra aqui — se precisar mudar o processo, edite `AGENTS.md`.
- Não há bloqueio técnico (hooks, gates) impedindo o Claude Code de gerar código fora do fluxo — a conformidade depende de o modelo seguir o protocolo declarado em `AGENTS.md` e de revisão humana no PR antes do merge.
